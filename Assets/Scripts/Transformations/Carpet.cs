using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Le personnage bloqué est considéré comme un element sol normale

public class Carpet : Transformation
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
}
